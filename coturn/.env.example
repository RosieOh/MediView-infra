# 서버의 공인 IP (필수). NAT 뒤라면 공인 IP 를 지정한다.
EXTERNAL_IP=203.0.113.10

# TURN realm (보통 도메인)
TURN_REALM=turn.mediview.example.com

# 백엔드 application 의 turn.static-auth-secret 과 "반드시 동일" 해야 한다.
# 생성 예: openssl rand -hex 32
TURN_SECRET=change-me-please-use-openssl-rand-hex-32
