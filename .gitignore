# 런타임/시크릿 (커밋 금지)
.env
.env.production
.env.staging
!*.example
certs/
certbot/conf/
certbot/www/

# Alertmanager 비밀값 (slack_url / smtp_password) — .example 만 커밋
monitoring/alertmanager/secrets/*
!monitoring/alertmanager/secrets/*.example

# Grafana nginx basic-auth htpasswd — .example 만 커밋
monitoring/nginx-secrets/*
!monitoring/nginx-secrets/*.example
